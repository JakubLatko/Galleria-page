---
import Slideshow from "../layouts/Slideshow.astro";
import SlideshowContent from "../components/SlideshowContent.astro";
import data from "../scripts/data.json";

const prefix = data[8];
---

<Slideshow
	isNextDisabled={false}
	isPreviusDisabled={false}
	painter={prefix.artist.name}
	paintingTitle={prefix.name}
	nextPainting={data[9].link}
	previousPainting={data[7].link}
	sliderPercentage={(100 * 9) / 15}
	title={"Galleria"}>
	<SlideshowContent
		description={prefix.description}
		painter={prefix.artist.name}
		painterPortrait={prefix.artist.image}
		paintingMobile={prefix.images.gallery}
		paintingDesktop={prefix.images.hero.large}
		source={prefix.source}
		title={prefix.name}
		year={prefix.year}
	/>
</Slideshow>
